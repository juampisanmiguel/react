import React from "react";
import Title from '../Title'


export const ItemListContainer = () => {
    return (
        <Title greeting= 'No hay productos disponibles' />
    );
}

export default ItemListContainer;